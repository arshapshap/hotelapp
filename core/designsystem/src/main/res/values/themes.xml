<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HotelsApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/blue700</item>
        <item name="android:colorBackground">@color/grey100</item>
        <item name="android:textAppearance">@style/HotelsApp.TextAppearance.Body1</item>
    </style>

    <style name="Theme.HotelsApp" parent="Base.Theme.HotelsApp" />
</resources>